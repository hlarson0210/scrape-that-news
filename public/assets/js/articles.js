// js file to...
// scrape
// add || delete note
// save articles
// view saved articles



// Make sure we wait to attach our handlers until the DOM is fully loaded.
$(document).ready(function() {
    $('.parallax').parallax();
    $('.sidenav').sidenav();

    $(".beer-me").on("click", () => {
        console.log("HEY");
    })

});